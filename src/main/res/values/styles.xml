<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <style name="common_dialog" parent="@android:style/Theme.Dialog">
        <!-- 内容覆盖物 -->
        <item name="android:windowContentOverlay">@null</item>
        <!-- 边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 半透明 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 无标题 -->
        <item name="android:windowNoTitle">true</item>

        <!--<item name="android:background">@null</item>-->
        <!-- 背景:透明,如果是@null，则没有了背景(应该是黑的??) -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 背景是否暗淡 周围变暗 -->
        <item name="android:backgroundDimEnabled">false</item>
    </style>
    <style name="BaseProject_Theme.Base" parent="Theme.AppCompat" >
        <item name="windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
    </style>
    <style name="common_dialog_style" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="login_dialog" parent="common_dialog_style">
        <item name="android:windowBackground">@drawable/rounded_white_shape</item>
    </style>
    <style name="style_4_popwindow_translate_y">
        <item name="@android:windowEnterAnimation">@anim/popwindow_y_translate_in</item>
        <item name="@android:windowExitAnimation">@anim/popwindow_y_translate_out</item>
    </style>
    <!-- 通用textView style:宽、高自适应；内部居中、外部居中；单行 -->
    <style name="common_tv_style">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:gravity">center</item>
        <item name="android:singleLine">true</item>
    </style>
    <style name="tv_fill_w_h_style" parent="common_tv_style">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
    </style>

    <style name="common_load_dialog_style" parent="android:Theme.Translucent">
        <item name="android:windowEnterAnimation">@anim/modal_in</item>
    </style>
    <!-- 加载进度条样式 -->
    <style name="circle_pb_loading_style">
        <item name="android:indeterminate">true</item>
        <item name="android:indeterminateDrawable">@drawable/indeterminate_progressbar_drawable</item>
        <item name="android:indeterminateDuration">1000</item>
        <item name="android:indeterminateOnly">true</item>
    </style>
</resources>